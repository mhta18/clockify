migrate notification signals to explicit views and add member status update

- Removed Django signals implementation from notification app
- Created 'TaskMemberUpdateSerializer' for member, restricting them to modify notification alone.
- Implemented "TaskMemberUpdateAPIView" allowing members update their own tasks easily.
- made websocket broadcasting communication in the view ,using channels.
- Fixed tests, working with websocket and mocking isolated path.

add job-schedule implementation:

created custum command line action for clearing notification after 30 days
used management/command as directory to implement customized command
made a test which create two instance of notification , then update created_at field for one of them to 32 days ago ,the result has to show one notification for specified user
 

-created a directory for the document_file field in the contract model.
-added two signals: first, to ensure that when a user is deleted, no files remain in the directory; second, to ignore redundant uploads of the same document file to the directory.
-updated the logic of the stop function in the TimeLog view to directly retrieve the log by ID, avoiding unnecessary validation and filtering.
-Created a new field called is_terminated in the Contract model to track the contract’s status.


-Removed Redis server configuration, used built-in in-memory channel layers userd for local testing
 
